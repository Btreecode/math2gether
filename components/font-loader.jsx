import { Kodchasan, Open_Sans } from "next/font/google";

const kodchasan = Kodchasan({
  subsets: ["latin"],
  weight: "600",
});

const openSans = Open_Sans({
  subsets: ["latin"],
  weight: "300",
});

export { kodchasan, openSans };
